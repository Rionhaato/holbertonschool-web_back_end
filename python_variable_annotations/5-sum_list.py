#!/usr/bin/env python3
"""Define a typed list summation helper."""

from typing import List


def sum_list(input_list: List[float]) -> float:
    """Return the sum of a list of floating-point numbers."""
    return sum(input_list)
